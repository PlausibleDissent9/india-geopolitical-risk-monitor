# Yahoo-derived data is re-fetched each run; only the GDELT store persists
# (backfill is expensive to redo -- see .github/workflows/daily.yml).
data/raw/prices.csv
data/raw/derived_returns.csv

__pycache__/
*.pyc
.pytest_cache/
.venv/
.DS_Store
ngram_*.log

# Session artifacts, never repo content
.claude/
chokepoint_backfill.log
*_dryrun.log
data/raw/naturalearth/
releases/
daily_local.log
heal_local.log

# Root-level session logs are scratch, never repo content (data/raw/last_backfill_failure.log stays tracked on purpose)
/*.log
